export const GrailsIcons = {
  // Custom Grails-specific icons
  grailsLogo: "$(symbol-misc)", // Will be custom SVG
  controller: "$(rocket)", // Controllers = actions/endpoints
  service: "$(gear)", // Services = business logic
  domain: "$(database)", // Domains = data models
  view: "$(browser)", // Views = web pages
  taglib: "$(tag)", // TagLibs = custom tags
  plugin: "$(extensions)", // Plugins
  urlMapping: "$(link)", // URL mappings
  config: "$(settings)", // Configuration

  // Status indicators
  serverRunning: "$(play-circle)",
  serverStopped: "$(stop-circle)",
  grailsProject: "$(folder-library)",
  artifact: "$(symbol-class)",
};
