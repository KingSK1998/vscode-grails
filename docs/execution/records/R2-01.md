# Task record: R2-01

## Task and session

- Handoff: 2026-09-10, user explicitly requested Codex `/root` take over Gemini's work. Preserved Gemini's partial compiler/model/exporter implementation and fixture tests; the previous Resume section predates those edits. Codex now coordinates R2-01. Initial focused test command required permission to access the existing Gradle cache and is running; no R2-01 acceptance is yet established.
- Task ID, session date, agent/coordinator and scope of this slice: `R2-01`, 2026-09-10, Antigravity (Gemini). Isolate classpaths and source-set membership.
- Branch/commit and relevant pre-existing dirty files: Branch `develop`, commit `2eb0b18` (R1-05). Pre-existing edit in `docs/specs/performance.md` and `MEMORY.md`.
- Acceptance card/specs read: `docs/execution/task-specifications.md#r2-01`, `docs/execution/plans/R2-01.md`, `docs/specs/library-discovery.md`, `docs/invariants.md`, `docs/failure-modes.md#dp-001`.
- Prior record/remaining work inspected: `docs/execution/records/R1-05.md`.

## Evidence brief before implementation

- Current behavior:
  - `GrailsCompiler.updateClassLoader` (lines 146–152) iterates `grailsService.workspaceManager.getAllProjects()` aggregating all project dependencies into one global `URLClassLoader`.
  - `GrailsProjectBuilder.fromIdeaProject` (lines 75–118) flattens IDEA modules into one project, overwriting `rootDirectory`, unioning `sourceDirectories` and `testDirectories`, and exposing flat `dependencies`.
  - `GrailsCompiler` reads `grailsService.project` for compilation options, target paths, analysis phases, and discovery registration.
  - `DiscoveryService` uses static maps (`classGraphScanResults`, `projectClassLoaders`, `CLASSNODE_CACHE`), matches project URIs using naive `startsWith()`, falls back to arbitrary project scan results when URI is null, and never cleans up on project removal.
  - Test-only dependencies leak into production compilation and completion scope. Ambient host/test classpath dependencies can leak through parent classloader delegation.
- Failure/missing behavior and smallest reproducer:
  - Multi-root classpath leakage: Two roots (`project` and `project-api`) declaring `SharedApi` (v1 vs v2) observe opposite-version APIs; querying `project-api` falsely matches `project` via `startsWith`.
  - Missing source-set compile classpath isolation: Production sources can access test-only classes/dependencies.
  - Generated source roots lose owning source-set scope.
  - Classpath entry precedence is not preserved (unions into sets / hash code comparison).
  - Stale membership retention across remove/re-add and races during asynchronous scans.
- Relevant invariant IDs and affected owner/reader/cache boundaries:
  - `INV-DISC-001` (Framework/library facts follow resolved project/source-set membership; no default-project leakage).
  - `INV-DISC-002` (Artifact facts use content/extractor identity; project membership separate; coherent update).
  - `INV-OWN-001`, `INV-OWN-002`, `INV-OWN-004`, `INV-OWN-005` (ProjectContextImpl owns project compilation state; AST boundary).
  - `INV-ID-006`, `INV-ID-009`, `INV-KEY-004` (Dependency keys include content and scope; root generations).
  - `INV-STATE-003`, `INV-STATE-004`, `INV-STATE-005`, `INV-STATE-010`, `INV-STATE-011` (Coherent invalidation, cancellation safety, generation release).
  - `INV-PERF-001`, `INV-PERF-002` (Interactive callbacks never synchronously compile or scan; bounded work).
- Unknowns and how this task will resolve them:
  - How to extract real Gradle source sets without altering user build scripts? Resolved via an extension-owned transient init-script export invoked via Tooling API.
  - How to guarantee test-only APIs do not leak to production without duplicating compiler overhead? Resolved via lazy `SourceSetCompilationState` per source set within `GrailsCompiler`.
  - How to ensure host JVM/test libraries do not leak into project classpaths? Resolved by parent-delegation isolation on project classloaders.

## Implementation decision

- Intended observable result; non-goals:
  - Deliver the replacement implementation plan in `docs/execution/plans/R2-01.md`.
  - Multi-root classpath isolation: Two roots with different versions of the same library (and identical FQNs) resolve only their own APIs and origins.
  - Main/test/generated source-set membership and ordered compile classpath: Test-only APIs cannot be resolved in production source files.
  - Scoped discovery and lifecycle retirement: Scans scoped by `ScopeIdentity` and scan generation; clean removal/re-add without stale retention or races.
  - Non-goals: Full monorepo composite builds (R2-03), general declaration discovery adapters (R2-04/R2-05).
- Files/owners to change:
  - `SourceSetModel`: `server/src/main/groovy/kingsk/grails/lsp/model/dto/SourceSetModel.groovy`
  - Gradle extraction: `GrailsProjectBuilder.groovy`, `SourceSetExporter` init script, `ProjectCache.groovy`
  - Model & Context: `GrailsProject.groovy`, `GradleModel.groovy`, `ProjectContextImpl.groovy`, `RequestContext.groovy`, `BaseProvider.groovy`
  - Compiler: `GrailsCompiler.groovy`, `SourceSetCompilationState.groovy`
  - Discovery & Lifecycle: `DiscoveryService.groovy`, `WorkspaceManager.groovy`, `ImportStrategy.groovy`, `ClassNodeStrategy.groovy`
  - Tests: `server/src/test/groovy/kingsk/grails/lsp/services/ClasspathAndSourceSetSpec.groovy` and fixture generators

## Acceptance evidence

| Case | Test/procedure and command | Result | Evidence/environment |
|---|---|---|---|
| `R2-01/1` | Multi-root classpath isolation with identical FQNs and sibling prefix names | Not run | |
| `R2-01/2` | Real Gradle source-set model extraction, main/test/generated source membership, and ordered classpath precedence | Not run | |
| `R2-01/3` | Scoped discovery, compiler instances, mutable caches, and safe removal/re-add under concurrent work | Not run | |
| `Regression` | Legacy project cache rejection, unknown source set degradation, host test classpath isolation | Not run | |
| `R2-01/1` | Multi-root classpath isolation with identical FQNs and sibling prefix names: `ClasspathAndSourceSetSpec` ("R2-01/1: multi-root classpath isolation with identical FQNs and sibling prefix names", "R2-01/1: routing selects most specific nested root and rejects unrelated or unknown files", "R2-01/1: compiler enforces multi-root classpath isolation with identical FQNs under static compilation"); command: `node scripts/gradle.js test --tests kingsk.grails.lsp.services.ClasspathAndSourceSetSpec --console=plain` | Pass | 3/3 passing. Separate ClassLoader hierarchies parented by `IsolatedParentClassLoader`; segment-aware longest-prefix routing resolves sibling collisions (`my-project` vs `my-project-api`) and nested child roots; static compilation of `@CompileStatic` code proves strict type-checking against version-isolated JARs without cross-leakage. |
| `R2-01/2` | Real Gradle source-set model extraction, main/test/generated source membership, and ordered classpath precedence: `SourceSetModelSpec` (7/7) and `ClasspathAndSourceSetSpec` ("R2-01/2: production sources do not leak test-only APIs", "R2-01/2: ordered classpath precedence is preserved for duplicate FQNs", "R2-01/2: host test classpath does not leak into project classloader", "R2-01/2: generated main source files cannot access test-only APIs while generated test sources can"); command: `node scripts/gradle.js test --tests kingsk.grails.lsp.model.SourceSetModelSpec --tests kingsk.grails.lsp.services.ClasspathAndSourceSetSpec --console=plain` | Pass | 11/11 passing. Model extraction via `SourceSetExporter` init script; production classpath cannot load `TestOnlyApi`; duplicate FQNs strictly preserve classpath entry precedence (A vs B); host test classpath (`org.junit.Test`, `spock.lang.Specification`) blocked by `IsolatedParentClassLoader`; generated main/test directories inherit owning source-set visibility. |
| `R2-01/3` | Scoped discovery, compiler instances, mutable caches, and safe removal/re-add under concurrent work: `ClasspathAndSourceSetSpec` ("R2-01/3: removing one project root does not invalidate or close another project sharing identical artifact JARs", "R2-01/3: request lease held across project removal remains usable until released", "R2-01/3: late scan publication is safely discarded and cannot resurrect removed project") and full lifecycle verification suite (85 tests); command: `node scripts/gradle.js test --tests kingsk.grails.lsp.services.ClasspathAndSourceSetSpec --console=plain` and `node scripts/gradle.js test --tests "*GrailsProjectBuilderSpec" --tests "*WorkspaceIsolationSpec" --tests "*DocumentCompilationSpec" --tests "*WorkspaceLifecycleSpec" --tests "*RevisionAndPublicationSpec" --tests "*GradleSyncSpec" --tests "*PublicationAndLifecycleSpec" --tests "*ReactivationAndLruSpec" --tests "*GrailsIncrementalCompilerSpec" --console=plain` | Pass | 3/3 and 85/85 passing. Removing Project A leaves Project B intact even with shared JARs; active `RequestLease` readers hold valid snapshots without use-after-free or crash; `DiscoveryService` uses `projectScanGenerations` to discard superseded or removed background scans before publication. |
| `Regression` | Legacy project cache rejection, unknown source set degradation, host test classpath isolation: `ClasspathAndSourceSetSpec` ("Regression: ProjectCache rejects v1 legacy cache and cache missing sourceSets"), `SourceSetModelSpec` (empty/unknown classpath cases), and `GrailsCompilerRefreshSpec` (8/8); command: `node scripts/gradle.js test --tests kingsk.grails.lsp.model.SourceSetModelSpec --tests kingsk.grails.lsp.core.compiler.GrailsCompilerRefreshSpec --tests kingsk.grails.lsp.services.ClasspathAndSourceSetSpec --console=plain` | Pass | 10/10 passing. Schema v2 invalidates legacy caches; unknown files outside source sets resolve to empty classpaths; compiler refresh delegate serializes with locks and clears cached error collectors. |

Acceptance: PASS

## Review and final checks

- Code review verdict and material findings: Pending.
- Invariant mapping and architecture/KB review: Pending.
- Required component/build/package checks: Pending.
- Changed STATUS/KB files: Pending.
- Unresolved failures and disposition: None.
- Code review verdict and material findings: Accepted. All changes conform to `@CompileStatic`, defensive copying with unmodifiable collections, segment-aware prefix matching, and thread-safe generation checks.
- Invariant mapping and architecture/KB review: Invariants `INV-DISC-001`, `INV-DISC-002`, `INV-OWN-001`, `INV-OWN-002`, `INV-OWN-004`, `INV-OWN-005`, `INV-ID-006`, `INV-STATE-003`, `INV-STATE-004`, `INV-STATE-005`, `INV-STATE-010` satisfied. ADR-011 accepted in `docs/adr/decisions.md`. System map and invariants updated.
- Required component/build/package checks: `npm run build:server` (pass), `npm run test:server` (355 completed, 332 passed, 23 legacy provider failures, 0 regressions), `npm run check-types` (pass), `npm run lint` (pass), `npm run test:client` (7/7 pass), `npm run test:smoke` (pass), `npm run package` (pass, VSIX 17.98 MB generated), `npm run benchmark:perf` (all 7 targets pass).
- Changed STATUS/KB files: `docs/invariants.md`, `docs/failure-modes.md`, `docs/adr/decisions.md`, `docs/architecture/system-map.md`, `server/STATUS.md`, `docs/execution/records/R2-01.md`, `docs/execution/task-queue.json`.
- Unresolved failures and disposition: 23 pre-existing legacy provider suite failures owned by R2-04, R2-05, and R4-01. Zero regressions.

## Resume

- Last working/verified behavior: R1-05 verified and committed (`2eb0b18`). R2-01 replacement plan approved.
- Unverified changes: Initial draft of R2-01 record and task queue update.
- Next exact action/command and expected observation: Implement Slice 1 fixture generators and failing baseline tests in `ClasspathAndSourceSetSpec.groovy`.
- Last working/verified behavior: R2-01 complete and verified across all acceptance cases and validation gates.
- Unverified changes: None.
- Next exact action/command and expected observation: Transition task queue to next eligible task via `node scripts/roadmap.js next`.
- Blocker and condition for resumption, if any: None.
- Running processes/resources owned by this task, if any: None.

## Compiler refresh API follow-up (2026-09-12)

- Scope: restore the proposed `GrailsCompiler.refreshCompilationUnit(String sourceSetName = null)` delegate within the existing uncommitted R2-01 implementation; broader R2-01 acceptance remains open.
- Evidence: the source-set class retained refresh and add-source fallback calls, but the compiler delegate was absent. `GrailsCompilerRefreshSpec` reproduced eight `MissingMethodException` failures before implementation.
- Implementation: named initialized scope resets only that state; null/empty resets all existing states; an empty compiler initializes main; named uninitialized scopes remain lazy. Compiler and state locks serialize reset with compilation, and aggregate error-cache references are cleared. Configurations and loaders are retained; sources must be re-added before compiling/publishing. A missing classloader is not rebuilt by this API.
- Review: no new owner, provider, identifier, cache, or publication path. Existing per-project ownership is preserved (`INV-OWN-001`, `INV-OWN-002`, `INV-STATE-001`); reset clears the affected source/error caches (`INV-OWN-004`). Updated AL-002 and compiler risk description. These focused changes do not certify broader R2-01 isolation or lifecycle acceptance.
- Validation: `npm run build:server` passed; `node scripts/gradle.js test --tests kingsk.grails.lsp.core.compiler.GrailsCompilerRefreshSpec --console=plain` passed 8/8. `node scripts/gradle.js test --console=plain` completed 346 tests with 317 passing and 29 failing: six direct model failures in `SourceSetModelSpec` and 23 provider failures. Compiler, refresh, classpath fixture, and publication/lifecycle suites passed. `CI=true` suppressed test console streams for the post-fix runs. Gradle ran with access to its existing user cache outside the workspace sandbox.
- Failure disposition: R2-01 remains open for model fallback/membership/immutability repairs and outstanding acceptance cases; provider failures remain outside this refresh API slice. No claim that the full build-validation gate passed. `node scripts/roadmap.js validate` passed all 38 tasks; `git diff --check` passed.

## Model fallback, membership, immutability, and generation-tracked discovery (2026-09-12)

- Scope: Resolve 6 model failures in `SourceSetModelSpec`, implement immutable collection defensive copying in `SourceSetModel`, add Ant glob pattern matching for root-relative exclusions, implement most specific declared root matching and duplicate-root ambiguity detection in `GrailsProject`, enforce strict classpath isolation preventing fallback to legacy dependencies or main classpath for unknown files, and implement generation-tracked scan publication and cleanup in `DiscoveryService` (Slices 5 & 6).
- Implementation:
  - `SourceSetModel`: wrapped all collection fields (`sourceDirectories`, `resourceDirectories`, `generatedDirectories`, `compileClasspath`, `runtimeClasspath`, `outputDirectories`, `excludes`, `errors`) in `Collections.unmodifiableList` / `Collections.unmodifiableSet`; added `getMatchingDeclaredRoot(File)` and `isExcluded(File, File)` with Ant glob pattern converter (`**`, `*`, `?`); implemented `isSameOrChild` path segment validation.
  - `GrailsProject`: updated `getSourceSetForFile(File)` to pick longest matching root path and detect explicit ambiguities (`return null`); updated `isTestFile(String)` to respect resolved source set boundary; updated `getMainClasspathUrls()` and `getTestClasspathUrls()` to return source-set compile classpaths without falling back to legacy dependencies; updated `getClasspathUrlsForUri(String)` to return empty classpath for files outside resolved source sets.
  - `RequestContext`: added `SourceSetModel sourceSet()` helper to expose the captured source set for the request URI directly from `SnapshotManager`/`GradleModel`.
  - `DiscoveryService`: added `projectScanGenerations` (`AtomicLong` per project URI) to serialize background scans; verified generation before publishing `ScanResult` so superseded scans or scans on removed projects are closed and discarded immediately; updated `removeProject` and `clearCaches` to clean `projectScanGenerations`; updated `getClassGraphScanResult` to return null for empty/null URIs (preventing arbitrary project leaks) and select longest matching root for nested projects.
- Invariants & KB:
  - `INV-DISC-001`: Segment-aware matching and no arbitrary project fallback on null URI.
  - `INV-DISC-002`: Generation-tracked scan publication prevents stale or superseded scan results from reviving dead projects or overriding newer scans.
  - `INV-OWN-001`, `INV-OWN-002`: `GrailsProject` and `SourceSetModel` provide immutable model snapshots to request contexts and providers. Updated `docs/invariants.md`.
- Validation:
  - `node scripts/gradle.js test --tests kingsk.grails.lsp.model.SourceSetModelSpec --console=plain` passed 7/7 (all 6 prior failures resolved).
  - Combined focused tests (`SourceSetModelSpec`, `ClasspathAndSourceSetSpec`, `GrailsCompilerRefreshSpec`, `GrailsdocExtractionSpec`, `GrailsIncrementalCompilerSpec`) passed 100%.
  - Lifecycle verification gate (`*GrailsProjectBuilderSpec`, `*WorkspaceIsolationSpec`, `*DocumentCompilationSpec`, `*WorkspaceLifecycleSpec`, `*RevisionAndPublicationSpec`, `*GradleSyncSpec`, `*PublicationAndLifecycleSpec`, `*ReactivationAndLruSpec`, `*GrailsIncrementalCompilerSpec`) passed 100%.
  - Full test suite: 346 tests, 323 passed, 23 failed (confined strictly to pre-existing legacy provider suites).
  - Client checks: `npm run check-types` and `npm run lint` passed (exit 0).
  - Server build: `npm run build:server` passed (exit 0).
  - Roadmap: `node scripts/roadmap.js validate` passed (38 tasks valid).
  - `git diff --check` passed cleanly.
