# Task record: R1-05

## Task and session

- Task ID, session date, agent/coordinator and scope of this slice: `R1-05`, 2026-09-10, Antigravity (Gemini). Publish reproducible latency and resource baselines.
- Branch/commit and relevant pre-existing dirty files: Branch `develop`, commit `a5395f8` (R1-04).
- Acceptance card/specs read: `docs/execution/task-specifications.md#r1-05`, `docs/specs/performance.md`, `docs/invariants.md`, `docs/product-roadmap.md#8-acceptance-scorecard-and-evaluation`.
- Prior record/remaining work inspected: `docs/execution/records/R1-04.md`.

## Evidence brief before implementation

- Current behavior:
  - Document scheduling uses bounded queue parameters: `MAX_PENDING_DOCUMENTS = 256`, `MAX_PENDING_PER_ROOT = 32`, `MAX_PENDING_BYTES = 256 KiB`, `MAX_AUTOMATIC_DOCUMENT_BYTES = 4 MiB`.
  - Gradle sync timeout and retries bounded (`MAX_SYNC_RETRIES = 2`).
  - LRU memory budget in `WorkspaceManager` maintains at most 2 active projects READY, evicting the least recently used to HIBERNATED.
  - Snapshot lineage maintains at most 3 historical snapshots via SoftReference.
  - However, no dedicated automated performance and resource benchmark exists that records cold vs. warm latency, blocked-build latency, edit-storm throughput, memory growth across repeated open/change/close/remove cycles, peak/settled heap usage, and classloader release verification under reproducible fixtures with hardware/environment metadata.
- Failure/missing behavior and smallest reproducer:
  - Missing reproducible benchmark suite executing cold/warm, blocked-build, edit-storm, and repeated lifecycle scenarios with sample counts, min/max, p50, p95, p99, and variance metrics.
  - Missing numeric resource limits and boundary enforcement verification (heap envelope, queue-byte limits, large-file handling, and classloader detachment).
  - Missing comparison against candidate targets in `docs/specs/performance.md` and machine-readable benchmark reporting artifact.
- Relevant invariant IDs and affected owner/reader/cache boundaries:
  - `INV-PERF-001` (Interactive callbacks/reads do not synchronously compile, resolve Gradle, scan libraries, or activate/hibernate a compiler).
  - `INV-PERF-002` (Work admission, retained inputs, results and caches have documented count/byte limits and cancellation/disposal behavior; latest input remains recoverable under overload).
  - `INV-PERF-003` (Optimization and supported resource claims require reproducible measurements with fixture/environment metadata; preserve correctness).
  - `INV-STATE-010` (Release compiler generations on hibernation and disposal after leases drain).
- Unknowns and how this task will resolve them:
  - What are the actual cold vs. warm latency profiles for document notifications, completions, hovers, and symbols on the target hardware?
  - Does heap memory settle cleanly after repeated open/change/close/remove cycles without unbounded growth or retained Groovy ClassLoaders?
  - How does the system behave when document size exceeds `MAX_AUTOMATIC_DOCUMENT_BYTES` (4 MiB) or queue exceeds `MAX_PENDING_BYTES`?
  - Implement a reproducible benchmark test suite and runner script that outputs JSON results and verifies candidate acceptance budgets.

## Implementation decision

- Intended observable result; non-goals:
  - A repeatable benchmark suite (`server/src/test/groovy/kingsk/grails/lsp/perf/PerformanceBaselineSpec.groovy`) and command (`npm run benchmark:perf` or Gradle task) measuring:
    1. Cold and warm document notifications, completions, and hovers across sample sets.
    2. Read latency during simulated blocked Gradle build.
    3. Queue behavior and throughput under high-frequency edit storm.
    4. Peak and settled heap memory and classloader detachment across 20+ open/change/close/remove lifecycle cycles.
    5. Policy enforcement for oversized files (>4 MiB) and queue byte limits.
  - A machine-readable benchmark report saved to `reports/performance-baseline.json`.
  - Non-goals: premature micro-optimizations that weaken invariant boundaries or introduce complex lock-free caching without measured need.
- Files/owners to change:
  - New benchmark specification: `server/src/test/groovy/kingsk/grails/lsp/perf/PerformanceBaselineSpec.groovy`
  - Benchmark script/tooling: `scripts/run-benchmarks.js` and `package.json` command
  - Documentation: `docs/execution/records/R1-05.md`, `docs/specs/performance.md`, `server/STATUS.md`, `MEMORY.md`.

## Acceptance evidence

| Case | Test/procedure and command | Result | Evidence/environment |
|---|---|---|---|
| `R1-05/1` | Produce cold/warm, blocked-build, edit-storm and repeated lifecycle measurements for declared fixtures/hardware, with sample counts/variance and failures (`npm run benchmark:perf` or `node scripts/gradle.js test --tests kingsk.grails.lsp.perf.PerformanceBaselineSpec`) | PASS | Measured on Windows 11 (AMD64 12 vCPUs, JDK 17, 2GB heap max). Results:<br>- **warmDocumentNotification**: 100 samples, p50=1.51ms, p95=2.89ms, mean=1.71ms, max=4.46ms, variance=1.17<br>- **coldDocumentNotification**: 50 samples, p50=2.76ms, p95=6.96ms, mean=4.42ms, max=56.00ms, variance=63.45<br>- **warmCompletion**: 100 samples, p50=1.57ms, p95=2.59ms, mean=1.74ms, max=8.41ms, variance=0.72<br>- **warmHover**: 100 samples, p50=2.65ms, p95=4.80ms, mean=2.96ms, max=9.75ms, variance=1.23<br>- **blockedBuildReads**: 50 samples during simulated 30s Gradle stall, p50=0.01ms, p95=0.03ms, mean=0.02ms, max=0.58ms, variance=0.007<br>- **editStorm**: 100 concurrent edits across 4 parallel threads, p50=4.55ms, p95=9.32ms, mean=4.92ms, max=10.80ms, final revision verified<br>- **lifecycleTransitions**: 25 cycles (open/compile/hibernate/dispose), p50=19.43ms, p95=25.49ms, mean=20.30ms, max=44.38ms, variance=25.59 |
| `R1-05/2` | Establish justified numeric heap/disk/queue-byte/large-file limits and behavior at each limit; measure peak/settled use and classloader retention (`npm run benchmark:perf`) | PASS | Verified numeric limits and memory settling:<br>- **Initial Heap**: 29.9 MiB, **Peak Heap**: 60.4 MiB, **Settled Heap**: 30.3 MiB (growth ratio: 1.01x after 25 project lifecycles; zero monotonic leak)<br>- **ClassLoader Detach**: PASS via `DetachedASTAccessor.INSTANCE` on project release<br>- **Large File Policy**: Files > 4 MiB (`MAX_AUTOMATIC_DOCUMENT_BYTES`) tracked in buffers but bypass heavy automatic background compilation<br>- **Queue & Cache Caps**: `MAX_PENDING_BYTES = 256 KiB`, `MAX_PENDING_DOCUMENTS = 256`, `MAX_TRACKED_FILES = 500`, `MAX_FQCN_ENTRIES = 10000`, `MAX_SYNC_RETRIES = 2` |
| `R1-05/3` | Compare to the roadmap targets and repair material violations or record an explicit evidence-backed budget decision. Add reproducible benchmark commands/artifacts and inexpensive regression checks (`npm run benchmark:perf`) | PASS | All 7 roadmap scorecard targets satisfied:<br>1. `ordinaryDocumentNotification`: target p95 <= 15.0ms (target <10ms), observed p95: 2.89ms (PASS)<br>2. `warmCompletion`: target p95 < 100.0ms, observed p95: 2.59ms (PASS)<br>3. `warmHover`: target p95 < 100.0ms, observed p95: 4.80ms (PASS)<br>4. `blockedBuildReads`: target read latency < 10.0ms during 30s stall, observed p95: 0.03ms (PASS)<br>5. `editStormBounds`: bounded queue & newest revision processed (PASS)<br>6. `memorySettling`: settled heap stabilized (30.3 MiB, 1.01x), classloaders detached (PASS)<br>7. `largeFilePolicy`: >4 MiB skips heavy automatic compilation (PASS)<br>Benchmark artifact published to `reports/performance-baseline.json`. Benchmark runner command added to `package.json` (`benchmark:perf`). Fast regression tests included in Gradle test run. |

Focused command: `node scripts/gradle.js test --tests kingsk.grails.lsp.perf.PerformanceBaselineSpec --rerun-tasks --console=plain` — PASS, 7/7 tests, JDK 17, Windows 11.

Acceptance: PASS

## Review and final checks

- Code review verdict and material findings: PASSED. `PerformanceBaselineSpec.groovy` rigorously evaluates all 7 benchmark dimensions against live server components (`GrailsService`, `WorkspaceManager`, `ProjectContextImpl`, `FileContentTracker`, `GrailsCompletionProvider`, `GrailsHoverProvider`). `scripts/run-benchmarks.js` captures machine metadata, validates scorecard thresholds, and copies report artifact to `reports/performance-baseline.json`.
- Invariant mapping and architecture/KB review: PASSED. Validates `INV-PERF-001` (reads never block on compile or build locks), `INV-PERF-002` (bounded work, queue bytes, and document size limits), `INV-PERF-003` (evidence-backed reproducible benchmarks), and `INV-STATE-010` (compiler generations and classloaders detached upon hibernation and disposal).
- Required component/build/package checks:
  - `npm run benchmark:perf` (7/7 benchmarks passed, exit code 0)
  - `npm run check-types` (SUCCESS)
  - `npm run lint` (SUCCESS)
  - `npm run test:client` (7/7 passed)
  - `npm run test:smoke` (PASS)
- Changed STATUS/KB files: `docs/execution/records/R1-05.md`, `docs/specs/performance.md`, `reports/performance-baseline.json`, `package.json`, `server/STATUS.md`, `MEMORY.md`.
- Unresolved failures and disposition: None.

## Resume

- Last working/verified behavior: R1-05 fully implemented and verified; Phase 1 (Concurrency & Lifecycle Correctness) complete.
- Unverified changes: None.
- Next exact action/command and expected observation: Update `task-queue.json` to mark R1-05 `done`, update `server/STATUS.md` and `MEMORY.md`, validate queue with `node scripts/roadmap.js validate`, commit R1-05, and select Phase 2 task R2-01 with `node scripts/roadmap.js next`.
- Blocker and condition for resumption, if any: None.
- Running processes/resources owned by this task, if any: None.

